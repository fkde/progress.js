{
  "scripts": {
    "build": "esbuild src/progress.js --bundle --minify --sourcemap --target=chrome58,firefox57,safari11,edge16 --outfile=build/progress.min.js"
  },
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}
